services:
  hodler-bot:
    build: .
    container_name: hodler-doc-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data          # БД и сгенерированные файлы
      - ./templates:/app/templates # Шаблоны DOCX
    environment:
      - PYTHONUNBUFFERED=1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
